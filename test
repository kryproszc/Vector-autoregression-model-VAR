  const renamedColumns = columns.map((col) => {
    let mapped = col;
    if (mapped === 'Projection A') mapped = labelA || mapped;
    if (mapped === 'Projection B') mapped = labelB || mapped;
    if (normalizeIbnrLabel) {
      mapped = mapped.replace(/\bIBNR\s*\+\s*RBNP\b/gi, 'IBNR');
    } else if (expandIbnrLabel) {
      mapped = mapped.replace(/\bIBNR\b(?!\s*\+\s*RBNP)/gi, 'IBNR + RBNP');
    }
    return mapped;
  });
