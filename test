from pathlib import Path

BASE_DIR = Path(__file__).resolve().parent

DATA_DIR = BASE_DIR / "data"
OUTPUT_DIR = BASE_DIR / "outputs"

DEVICE = "cpu"

EMBEDDING_MODELS = [
    str(BASE_DIR / "models" / "stella-pl-retrieval-8k"),
]

RERANKER_MODELS = [
    str(BASE_DIR / "models" / "polish-reranker-roberta-v3"),
]

BASE_CONFIG = {
    "dense_top_k": 60,
    "bm25_top_k": 60,
    "rerank_top_k": 40,
    "final_top_k": 5,

    "embedding_batch_size": 8,
    "reranker_batch_size": 4,

    "answer_max_chunks": 3,
    "answer_sentences_per_chunk": 2,

    "chunk_max_words": 120,
    "chunk_overlap_words": 30,

    "bm25_weight": 1.2,
    "rrf_k": 60,
}



python rag_optimizer.py
