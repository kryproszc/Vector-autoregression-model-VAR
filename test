sciezka_bazy = r"C:\twoj\folder\baza_danych.db"
nazwa_tabeli = "dane_raportowe"

conn = sqlite3.connect(sciezka_bazy)

df.to_sql(
    nazwa_tabeli,
    conn,
    if_exists="append",   # dopisuje dane do istniejącej tabeli
    index=False
)

conn.close()
